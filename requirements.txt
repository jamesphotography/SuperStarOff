# SuperStarOff - Photoshop 插件依赖
# Python 3.11 required（numpy<2.0 仅提供至 3.12 的 wheel，3.13+ 装不上）

# 核心深度学习
torch>=2.0.0

# 图像处理
numpy>=1.24.0,<2.0
Pillow>=10.0.0
tifffile>=2023.0.0
imagecodecs>=2023.0.0  # TIFF 压缩格式支持

# 模型加密
cryptography>=41.0.0
